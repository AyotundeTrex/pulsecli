"""
Takes the calculated metrics and prints them as a clean, readable report.

Responsibilities (implemented in Stage 6):
- Format the final results into the "LOAD TEST RESULTS" summary block
- Keep this separate from metrics.py so the calculation logic and the
  display logic don't get tangled together

Left empty on purpose for Stage 1.
"""
