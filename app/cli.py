"""
Entry point for the command line.

This is where the program starts when you run:
    python -m app.cli --url https://example.com --users 10 --duration 30

Responsibilities (implemented in Stage 2):
- Parse command-line arguments (--url, --users, --duration, --timeout, etc.)
- Build a Config object from those arguments
- Kick off the test run and print the final report

Left empty on purpose for Stage 1 — this file exists so the project
structure is real and importable, but no logic lives here yet.
"""
